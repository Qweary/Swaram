Resume the AudioHax engagement by reading the engagement head and reporting a short orientation.

Read `SWARM-STATE.md` at the repo root. Then report back in three labelled parts, kept brief:

WHERE WE ARE: summarize the `## State` snapshot in 2-3 lines (engagement, current session, active workstreams).
WHAT'S BLOCKED: list any blockers or gates noted in `## State` (e.g. workstreams waiting on a source release or an upstream fix). If none, say so.
WHAT'S NEXT: report the `## Next Session` content verbatim — the next kickoff prompt(s). If it is still the placeholder, say the lead has not yet authored the next kickoff.

If `SWARM-STATE.md` is missing, say so plainly and point the operator to the engagement HEAD as the source of truth. Do not invent state.
